/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppUIStyler from './AppUIStyler';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppUIStyler);
